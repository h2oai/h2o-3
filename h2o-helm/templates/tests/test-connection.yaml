apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "h2o-helm.fullname" . }}-test-connection"
  labels:
    {{- include "h2o-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "h2o-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
