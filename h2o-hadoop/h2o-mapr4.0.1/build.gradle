apply plugin: 'fatjar'

description = "H2O for Hadoop mapr4.0.1"

sourceCompatibility = 1.6
targetCompatibility = 1.6

compileJava { 
  options.debug = true 
}

sourceSets {
  main {
    java {
      srcDir hadoopSrcDir
    }
  }
}

dependencies {
  compile("org.apache.hadoop:hadoop-client:2.4.1-mapr-1408")
  compile project(hadoopDependentProject)
  compile files(hadoopPersistHdfsJarFile)
}

fatJarPrepareFiles {
  exclude hadoopFatJarExcludes
}

fatJar {
   manifest {
      attributes 'Main-Class': hadoopFatJarMainClass
   }
}

jar.finalizedBy fatJar
