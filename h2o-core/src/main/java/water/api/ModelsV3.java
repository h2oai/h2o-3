package water.api;

import water.api.ModelsHandler.Models;

class ModelsV3 extends ModelsBase {
  // Version-  and Schema-specific filling into the impl
  @Override public Models createImpl( ) {
    return super.createImpl();
  }

  // Version-  and Schema-specific filling into the impl
  @Override public ModelsBase fillFromImpl(Models f) {
    return super.fillFromImpl(f);
  }

}
