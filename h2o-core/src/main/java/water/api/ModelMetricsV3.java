package water.api;

import water.ModelMetrics;

public class ModelMetricsV3 extends ModelMetricsBase {
  // Version-  and Schema-specific filling into the impl
  @Override public ModelMetrics createImpl( ) {
    return super.createImpl();
  }

  // Version-  and Schema-specific filling into the impl
  @Override public ModelMetricsBase fillFromImpl(ModelMetrics f) {
    return super.fillFromImpl(f);
  }

}
